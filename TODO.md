# TODO

- [x] Assign a unique gradient per topic — gradients stored in Sanity on each topic document (`gradientFrom`/`gradientTo`), fetched via GROQ and applied as inline styles
- [ ] Calculate `readTime` automatically from article body content once the body field is added to the Sanity article schema
- [ ] Build a custom Sanity Studio tool/plugin to view all sections used across pages in one place (dashboard-style overview)
