# TODO

- [ ] Assign a unique gradient per topic — once topics are finalized, generate a gradient for each and map them in `GRADIENT_MAP` (`ArticleCardGrid.tsx`) and `ArticleCardGrid.scss`
- [ ] Calculate `readTime` automatically from article body content once the body field is added to the Sanity article schema
